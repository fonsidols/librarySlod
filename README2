CLASE DE GITHUB


git config --global user.name "ALFONSO"
git config --global user.email ""

GENERANDO TU PUBLIC key

SSH-KEYGEN

Leyendo tu clave para copiarla a GitHub

cat ~/.ssh/id_rsa.pub

Arrancando el proyecto:

git init

touch README

git add README

git commi -m "tu primer commit"

git push origin master